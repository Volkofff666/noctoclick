# NoctoClick Dashboard (Frontend)

React-приложение для управления системой защиты от скликивания.

## 🚀 Быстрый старт (без backend)

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в MOCK режиме

По умолчанию приложение работает с **фейковыми данными** (без backend и БД).

```bash
npm run dev
```

Откроется: `http://localhost:3000`

### 3. Тестирование

**Логин/пароль может быть любым** - mock API всегда возвращает успех.

Рекомендуемые данные для теста:
- Email: `demo@noctoclick.ru`
- Пароль: `password`

**Доступные страницы:**
- `/login` - Вход
- `/register` - Регистрация
- `/sites` - Мои сайты (3 демо-сайта)
- `/dashboard` - Дашборд со статистикой
- `/blocked` - Заблокированные IP
- `/settings` - Настройки
- `/profile` - Профиль пользователя

---

## 🔧 Переключение на реальный API

Когда backend будет готов:

1. Открой файл `src/utils/api.js`
2. Измени переменную:
   ```javascript
   const USE_MOCK_API = false; // было true
   ```
3. Убедись, что backend запущен на `http://localhost:3001`
4. Перезапусти `npm run dev`

---

## 📦 Структура проекта

```
src/
├── pages/              # Страницы
│   ├── Login.jsx       # Вход
│   ├── Register.jsx    # Регистрация
│   ├── Sites.jsx       # Мои сайты
│   ├── Profile.jsx     # Профиль
│   ├── Dashboard.jsx   # Статистика
│   ├── BlockedIPs.jsx  # Блокировки
│   └── Settings.jsx    # Настройки
├── components/
│   └── Layout/         # Сайдбар + Header
├── utils/
│   ├── api.js          # API client (переключатель)
│   └── mockApi.js      # Mock данные
└── styles/
    └── global.css      # Глобальные стили
```

---

## 🎨 Фичи в Mock режиме

✅ Работает весь UI
✅ Можно добавлять/удалять сайты (в памяти)
✅ Редактирование профиля
✅ Смена пароля
✅ Добавление/удаление заблокированных IP
✅ Переключение между сайтами
✅ Просмотр статистики (фейковой)

---

## 🐛 Если что-то не работает

### Проблема: "Cannot find module"

```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Порт 3000 занят

В `vite.config.js` измени порт:

```javascript
export default {
  server: {
    port: 3001 // или другой
  }
}
```

---

## 📸 Скриншоты

После запуска увидишь:

1. **Страница входа** - красивая форма с градиентом
2. **Мои сайты** - 3 демо-сайта с карточками
3. **Профиль** - форма редактирования данных
4. **Дашборд** - графики и статистика

---

## 🔥 Production Build

```bash
npm run build
npm run preview
```

Собранные файлы будут в папке `dist/`